import { styled } from "@nextui-org/react";

export const Box = styled("div", {
  background: "$ctd_services_bg_color",
  paddingTop: "$18",
  paddingBottom: "$18",
});

export const CardSection = styled("div", {
  display: "flex",
});
